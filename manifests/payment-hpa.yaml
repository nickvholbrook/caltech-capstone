apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  creationTimestamp: null
  name: php-apache
spec:
  maxReplicas: 10
  minReplicas: 1
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: php-apache
  targetCPUUtilizationPercentage: 50
  targetMemoryUtilizationPercentage: 50
status:
  currentReplicas: 0
  desiredReplicas: 0

  # kubectl run -i --tty load-generator --rm --image=busybox:1.28 --rO- http://php-apache; done""while sleep 0.01; do wget -q -O